<% include _header %>

<h1>This will be a page listing every posts... dummy content 4 now...</h1>
<% posts.forEach(function(post) { %>


<div class="panel panel-default">
            <div class="panel-heading">
              <h3 class="panel-title"> Title : <a href="/posts/<%= post.slug %>">
       <%= post.title %>
      </a></h3>
            </div>
            <div class="panel-body">
         
    <% if (posts.body) { %>
<p><bold> Body :</bold> </p>
    <%= post.body %>
    <% } %>
              </div>
          </div>

<% }) %>

<% include _footer %>
